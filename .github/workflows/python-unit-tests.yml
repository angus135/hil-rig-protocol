name: python-unit-tests

on:
  push:
    branches:
      - main
      - develop
      - 'feature/**'
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Python tests placeholder
        run: |
          echo "Python protocol bindings/tests are not implemented yet."
          echo "Passing this workflow as a temporary placeholder."
          exit 0
